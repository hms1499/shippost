/**
 * Shared page geometry.
 *
 * Mobile and tablet stay on the focused 448px rail used by the transactional
 * flow. At the desktop breakpoint the page opens into a 1152px, 12-column
 * shell while long-form copy and navigation remain on the narrower 896px rail.
 */
export const PAGE_SHELL_CLASS = 'w-full max-w-md lg:max-w-6xl';

export const CONTENT_RAIL_CLASS = 'w-full lg:max-w-4xl lg:mx-auto';
